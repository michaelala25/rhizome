"""Runnable examples for ``rhizome.tui.widgets.shared``.

Small, self-contained Textual apps that exercise a shared widget end to end. Like a real consumer, each one
builds its own data, adapts it to the widget's descriptors, and drives the widget — the widgets themselves
stay ignorant of where their data comes from.

Run one as a module, e.g.:  uv run python -m rhizome.tui.widgets.shared.examples.merge_tree
"""
